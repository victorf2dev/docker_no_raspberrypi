# Generated by setupmc.com on 2025-06-22T20:06:31.494Z
# Com suporte do Copilot (Microsoft)
#
# `docker compose up -d` para iniciar o container
# `docker compose down` para interrromper o container
# `sudo lsof -i -P -n | grep 25565` para consultar execucao na porta 25565
# `docker logs -f nome-do-container --tail 5` para vizualizar os ultimas 5 linhas delog


services:
  mc:
    image: itzg/minecraft-server:latest
    tty: true
    stdin_open: true
    ports:
      - "25565:25565" # porta padrao
    container_name: nome-do-container
    restart: unless-stopped
    environment:
      EULA: "TRUE"
      VERSION: "1.21.8" # versao do jogo
      MEMORY: "3072M" # memoria disponivel para o servidor
      MAX_PLAYERS: "20" # numero maximo de players
      MOTD: "apresentacao do servidor"
      DIFFICULTY: "2" # normal
      PVP: "false" # desativa modo PVP
      SEED: "seed do mapa"
      LEVEL: "nome do mapa"
      ENABLE_AUTOPAUSE: "false"
      OPS: |-
        nomes_de_operadores
      ENABLE_WHITELIST: "true" # habilitar lista para usuarios permitidos
      WHITELIST: |-
        nomes_de_usuarios
    volumes:
      - "./data:/data"
